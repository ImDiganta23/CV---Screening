services:
  - type: web
    name: cv-screening-api
    runtime: python
    buildCommand: ""
    startCommand: uvicorn api.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: MISTRAL_API_KEY
        sync: false
